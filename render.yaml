services:
  - type: web
    name: ai-career-ecosystem
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SESSION_TTL_HOURS
        value: 24
      - key: PYTHON_VERSION
        value: 3.11.9
